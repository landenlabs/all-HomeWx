<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    android:background="@drawable/bg_forecast_card"
    android:orientation="vertical"
    android:padding="10dp">

    <TextView
        android:id="@+id/pastCardTimeText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="6dp"
        android:gravity="center"
        android:textColor="@color/accent_cool"
        android:textSize="14sp"
        android:textStyle="bold"
        tools:text="Tue 3:30 PM" />

    <!-- Icon is match_parent height so it fills whatever height the value stack ends up at. -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/pastCardIcon"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:adjustViewBounds="true"
            android:contentDescription="@null"
            android:scaleType="fitCenter"
            tools:src="@drawable/wx_sun_44d" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="10dp"
            android:gravity="center_vertical"
            android:orientation="vertical">

            <TextView
                android:id="@+id/pastCardTempText"
                style="@style/forecast_card_value"
                tools:text="68°F" />

            <TextView
                android:id="@+id/pastCardWindText"
                style="@style/forecast_card_value"
                tools:text="12 mph" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/pastCardPrecipText"
                    style="@style/forecast_card_value"
                    android:layout_width="wrap_content"
                    tools:text="0.12 in" />

                <ImageView
                    android:id="@+id/pastCardPrecipIcon"
                    android:layout_width="wrap_content"
                    android:layout_height="16dp"
                    android:layout_marginStart="4dp"
                    android:adjustViewBounds="true"
                    android:contentDescription="@null"
                    android:scaleType="fitCenter"
                    android:src="@drawable/wx_drop_1"
                    android:visibility="gone" />
            </LinearLayout>

            <TextView
                android:id="@+id/pastCardPressureText"
                style="@style/forecast_card_value"
                tools:text="29.92 inHg" />
        </LinearLayout>
    </LinearLayout>

</LinearLayout>
